---
name: intranet
description: "Lightweight local HTTP file server with plugin support via symlinks. Use for serving HTML dashboards, documentation, or web content generated by other skills. Start/stop/status management with PID tracking."
summary: "Local HTTP file server with symlink-based plugin support."
version: 1.0.0
homepage: https://github.com/odrobnik/intranet-skill
metadata:
  openclaw:
    emoji: "🌐"
    requires:
      bins: ["python3"]
---

# Intranet

Lightweight local HTTP file server. Other skills can plug in content via symlinks.

**Entry point:** `{baseDir}/scripts/intranet.py`

## Setup

See [SETUP.md](SETUP.md) for prerequisites and setup instructions.

## Commands

```bash
python3 {baseDir}/scripts/intranet.py start                          # Start on default port 8080
python3 {baseDir}/scripts/intranet.py start --port 9000              # Custom port
python3 {baseDir}/scripts/intranet.py start --dir /path/to/webroot   # Custom root directory
python3 {baseDir}/scripts/intranet.py start --host localhost          # Bind to localhost only
python3 {baseDir}/scripts/intranet.py status                         # Check if running
python3 {baseDir}/scripts/intranet.py stop                           # Stop server
```

## Plugin Support

Other skills add content by symlinking directories into the webroot:

```bash
ln -s ~/clawd/deliveries ~/clawd/intranet/deliveries
```

Served at `http://localhost:8080/deliveries/`. The server follows symlinks and supports CGI scripts (executable `.py` files).

## Notes
- PID file: `~/.intranet.pid`
- Config file: `~/.intranet.conf`
- Default root: `{workspace}/intranet/` (override with `--dir` or `INTRANET_DIR` env var)
- Binds to `0.0.0.0` by default (all interfaces)
